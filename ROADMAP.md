# Roadmap

記錄這個專案做到哪、接下來想往哪走。

## 已完成

- **核心流程**：貼 JD → 呼叫 Claude 對照預設履歷 → 產出匹配分析 → 記錄 token 用量與成本
- **LLM 回應容錯**：模型偶爾會把 JSON 包在 code fence 裡或前後夾雜說明文字，改成逐層退讓解析
- **分析中的等待回饋**：分析要等 10～30 秒，送出後按鈕會變成「分析中」並停用，避免重複送出
- **履歷 PDF 上傳**：透過 `pdftotext` 抽出文字帶進表單，不保存原始檔
- **104 自動帶入改為輔助**：對方有 Cloudflare 擋著，伺服器端請求拿不到內容，因此把手動貼上改為主要方式
- **測試**：model / service / request / system 四層，89 個 example
- **靜態檢查**：`bin/check` 一次跑完 RuboCop、Brakeman、bundler-audit
- **修掉 `source_url` 的 XSS**：只放行 `http(s)` 開頭的網址才輸出成連結
- **Rails 7.2 → 8.1 升級**：7.2 的安全性支援 2026-08-09 到期，趕在期限前升上去

## 接下來（還沒決定做哪個）

- **A. 更好用**
  投遞狀態追蹤（投遞 → 面試 → 結果）、自我介紹草稿匯出成 Markdown、分析時可指定用哪份履歷
- **B. 成本報表**
  目前只在首頁顯示本月花費。`usage_records` 的資料夠做更細的拆解：依 model、依日期、依職缺，加上趨勢
- **C. 分析品質的量測**
  建一組標準答案，用它來量「分析結果準不準」，換 prompt 或換 model 時才有依據判斷是變好還是變差
- **D. 其他**
  用 headless browser 繞過 104 的 Cloudflare、部署上線

## 已知問題

見 [README 的「已知限制」](README.md#已知限制)。
